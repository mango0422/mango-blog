# Part 2 요약노트 — 정규화·관계·트랜잭션 심화 (SQLD 1과목 후반부)

생성일: 2025년 11월 12일 오후 10:28

### 🧩 1-6 정규화 심화 (Normalization)

**개념:**

- 중복 데이터를 제거하여 **저장 공간 효율과 데이터 일관성**을 확보.
- 데이터 이상현상(삽입·갱신·삭제)을 방지하는 데이터 모델링 기법.

**정규화의 목적**

1. 데이터 중복 제거
2. 데이터 독립성 확보
3. 이상현상(Anomaly) 방지

**이상현상 3종**

| 구분 | 설명 | 예시 |
| --- | --- | --- |
| 삽입이상 | 불필요한 데이터까지 입력 필요 | 사원 추가 시 부서정보도 강제 입력 |
| 갱신이상 | 동일 데이터 다수 존재, 일부만 수정 | 부서명 변경 시 일부만 수정됨 |
| 삭제이상 | 특정 데이터 삭제 시 관련 데이터까지 삭제 | 부서 삭제 시 사원정보도 소실 |

**정규화 단계 요약**

| 단계 | 내용 | 핵심 개념 |
| --- | --- | --- |
| 1NF | 속성이 원자값(Atomic)이어야 함 | 반복속성 제거, 행 분리 |
| 2NF | 부분 종속 제거 | 복합키 일부에 종속된 속성 분리 |
| 3NF | 이행 종속 제거 | A→B, B→C → A→C 분리 |
| BCNF | 결정자가 후보키가 되도록 | 후보키 아닌 결정자 제거 |
| 4NF | 다중값 종속 제거 | 한 속성이 여러 컬럼을 종속시키는 경우 분리 |
| 5NF | 조인 종속 제거 | 조인 시 중복 발생하는 경우 분리 |

**반정규화(De-Normalization)**

- 성능 향상을 위해 정규화를 역으로 수행하여 **조인 횟수를 줄이는 방법.**
- 장점: 조회 성능 향상 / 단점: 데이터 일관성 저하, 중복 증가

**반정규화 수행 사례**

- 다량 범위 처리, 특정 집계/요약 빈번, 조회 속도 저하 시

---

### 🔗 1-7 관계와 조인의 이해

**관계(Relationship)**

- 엔터티 간 인스턴스의 논리적 연관성.
- 부모 엔터티의 식별자를 자식 엔터티로 상속하여 **조인키(매핑키)** 로 활용.

**관계 유형**

- 존재 관계: 상태 기반(예: 사원–부서)
- 행위 관계: 이벤트 기반(예: 고객–주문)

**조인(Join)의 개념**

- 분리된 테이블을 연결하여 데이터를 조회하는 과정.
- **정규화로 분리된 데이터 간의 관계 복원.**

**조인 예시 (계좌–관리점)**

```sql
SELECT A.계좌번호, B.관리점
FROM 계좌 A, 관리점 B
WHERE A.관리점코드 = B.관리점코드
AND A.계좌번호 = '100111';

```

**계층형 데이터 모델 (Self Join)**

- 같은 테이블 내 인스턴스 간 계층 구조.
- 예: 사원(EMP) 테이블의 MGR 컬럼이 상사의 사번을 참조.

**상호배타적 관계**

- 두 엔터티 중 하나만 상속 가능한 관계 (예: 개인고객 vs 법인고객)

**ERD 표기법 요약**

- IE 표기법: 선택 관계(원), 필수 관계(무원)
- 바커 표기법: 점선(선택), 실선(필수)

---

### 🔄 1-8 모델이 표현하는 트랜잭션의 이해

**트랜잭션(Transaction)**

- 하나의 연속된 논리적 업무 단위.
- 특징: **All or Nothing**, 원자성 보장.

**예: 송금 트랜잭션**

1. A 계좌 잔액 조회 (>= 송금액)
2. A 계좌 잔액 차감
3. B 계좌 잔액 가산
    
    → 2번, 3번은 반드시 함께 성공(COMMIT) 또는 함께 실패(ROLLBACK)
    

**트랜잭션 설계 시 주의**

- 부분 커밋 불가
- 독립적 실행 금지

**ERD 상 표현**

- **필수 관계**: 하나의 트랜잭션으로 묶임 (실선)
- **선택 관계**: 독립 수행 가능 (점선)

---

### ⚙️ 1-9 NULL 속성의 이해

**NULL의 정의**

- 아직 정의되지 않은 값 (0, ''과 다름)
- 연산 시 주의 필요 (NULL 포함 시 결과 NULL)

**특징**

1. NULL 포함 연산 결과 = NULL
2. 그룹 함수는 NULL 무시 (SUM, AVG, COUNT 등)
3. `COUNT(*)` 는 NULL 포함, `COUNT(col)` 은 제외

**예시**

```sql
SELECT COUNT(*), COUNT(COMM), SUM(COMM), AVG(COMM)
FROM EMP;

```

- COUNT(*) → 전체 행 수
- COUNT(COMM) → NULL 제외한 행 수
- AVG(COMM) → NULL 제외 평균

**NULL 표기법**

- IE 표기법: NULL 여부 구분 불가
- Barker 표기법: 속성 앞 동그라미 = NULL 허용

---

### 🧮 1-10 본질식별자 vs 인조식별자

**식별자 구분**

| 구분 | 본질식별자 | 인조식별자 |
| --- | --- | --- |
| 정의 | 업무적으로 의미 있는 자연키 | 인위적 생성 키 (예: 일련번호) |
| 장점 | 업무 일관성 유지 | 관리·성능 측면 단순 |
| 단점 | 복합키로 복잡 | 데이터 중복·인덱스 낭비 |

**예: 주문 상세 설계**

- 본질식별자: (주문번호 + 상품번호)
- 인조식별자: 주문상세번호 생성 → 중복 가능성↑, 인덱스 낭비 가능

**주의**

- 인조식별자 사용 시 불필요한 INDEX 증가로 **DML(INSERT/UPDATE/DELETE)** 성능 저하 발생 가능.

---

✅ **핵심 암기 문장**

- **정규화는 이상현상을 제거**하고, **반정규화는 성능 향상 목적**으로 수행한다.
- **조인은 분리된 테이블 간의 논리적 복원 과정**이다.
- *트랜잭션은 원자성(Atomicity)과 일관성(Consistency)**을 보장해야 한다.
- **NULL은 값이 아님**, 연산과 집계 시 별도 처리 필수.
- **인조식별자는 편의성은 높지만, 품질 저하 위험**이 있다.